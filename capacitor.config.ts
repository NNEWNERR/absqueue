import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.absqueue',
  appName: 'absqueue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
